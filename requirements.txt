# Core - required
windows-capture>=1.4.2    # Windows.Graphics.Capture bindings (per-window capture)
opencv-python>=4.9        # crop / resize / fingerprinting
numpy>=1.24
pywin32>=306              # window enumeration, overlay click-through
winsdk>=1.0.0b10          # Windows.Media.Ocr - the default OCR engine

# Optional - only needed for `"ocr": {"engine": "rapidocr"}`
# Use this if Windows OCR has no language pack for your game's language.
# rapidocr-onnxruntime>=1.3.24

# Packaging constraint: Pillow lands here as rapidocr's dependency, and
# 12.x compiles (on Python 3.10) into bytecode that crashes stdlib dis --
# which is what PyInstaller walks imports with, so builds die with
# "IndexError: tuple index out of range" deep in modulegraph.
pillow<12
